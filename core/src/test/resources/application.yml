spring:
  datasource:
    url: jdbc:mysql://localhost:3306/table?allowMultiQueries=true&characterEncoding=utf8
    username: name
    password: password
  application:
    name: api

server:
  port: 8080
  servlet:
    context-path: /api

redis:
  pool:
    type: standalone
    address: localhost:6379
    password: password